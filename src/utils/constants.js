export const userRolesEnum = {
    ADMIN : "admin",
    PROJECT_ADMIN : "project_admin",
    MEMBER : "member"
}

export const AvailableUserRoles = Object.values(userRolesEnum);     // suggests enum values as arrays

export const taskStatusEnum = {
    TODO : "todo",
    IN_PROGRESS : "in_progress",
    DONE : "done"
}

export const AvailableTaskRoles = Object.values(taskStatusEnum);     // suggests enum values as arrays
